name: Workflow Testing
on:
  schedule:
    - cron: '1-59/6 * * * *'
jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: echo "Running Perfectly.................................."
      - name: Second Step
        run: python --version
